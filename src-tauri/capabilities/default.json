{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main SkillDeck window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
